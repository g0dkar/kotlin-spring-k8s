bind 0.0.0.0
port ${PORT}
cluster-enabled yes
cluster-config-file nodes.conf
cluster-node-timeout 5000
cluster-announce-ip ${IP}
cluster-announce-bus-port ${BPORT}
cluster-announce-port ${HPORT}
appendonly yes
dir /redis-data/${PORT}
